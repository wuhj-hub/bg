# 📋 体系更新日志 · 2026-09-12

> 主题：**一统天下·RSV50三线相对强度** 体系落地（扫描器 → workflow → 盘前报告 全链路）

---

## 一、本次变更（3 个文件）

### 1. 扫描器 `quant_scripts/yitong_screener.py`
- 新增 **月线反转检测** `detect_reversal()`：平台突破12月新高 / 均线金叉 / 趋势确立
- 新增 **RSV50三线相对强度**（Step2.6）：`个股50日涨幅 > 行业50日涨幅 > 大盘50日涨幅`
- 新增 **路径兼容层** `BASE`：沙箱 `/sandbox/workspace` ↔ GitHub runner 仓库根(cwd)
- 输出新增：主表「RSV50强度」列 + `⭐建仓区+RSV50三线强度`专表 + `🔥月线反转+RSV50双共振`专表
- 股池 `yitong_pool.txt`：加统计行 + `+反转(xxx)` / `+RSV50强` 标记

### 2. 盘前报告 `quant_scripts/gen_premarket_report.py`
- 新增 **③.5 一统天下·RSV50三线强度** 节（渲染 `render_yitong_rsv50()`）
- 位置链：③.3 板块主线 → ③.4 宁静卡位链 → **③.5 RSV50** → ④ 个股
- 缺数据静默跳过（不影响报告）

### 3. workflow `.github/workflows/quant_scan.yml`
- 新增步骤「一统天下多周期扫描」（置于 run_all_quant 之后）
- 提交列表新增：`一统天下建仓区股池_latest.json` / `_latest.md` / `yitong_pool.txt`

---

## 二、本次会话回测结论汇总

| 标的/策略 | 结论 |
|---|---|
| 绿线指标（Wish Green Line）| ❌ 负 alpha（前高=危险区，非买点）|
| 月线反转 + MACD底背离 | ⚠️ 仅持 12 月+ 有效（胜率 50%→52%）|
| 缠论分型策略（周线→日线）| ✅ 牛市 64.7% / 震荡 62.5% / 熊市 -1.55%（靠止损守住）|
| 三线共振（当日涨幅）+ 建仓区 | ✅ 20日 +2.25%（超额 +2.82pct）|
| **RSV50三线强度 + 建仓区** | ✅✅ **20日 +5.37%（超额 +5.87pct）· 胜率 62-70% · 盈亏比 1.67** |

**核心结论**：用 **50日相对强度**（而非单日涨幅）衡量"个股>行业>大盘"，再叠加"一统天下建仓区（低位）"，是最强的选股组合。

---

## 三、数据流闭环

```
盘后 15:05 全盘量化(quant_scan)
  └→ 一统天下扫描(yitong_screener)
       └→ 建仓区 301 只 → 月线反转 5 只 / RSV50强度 28 只 / 双共振 4 只
            └→ 提交: 一统天下建仓区股池_latest.json/md + yitong_pool.txt
                 └→ 次日盘前 06:30/08:00 盘前报告 ③.5 引用
```

---

## 四、体系自检结果（2026-09-12）

| 项 | 结果 |
|---|---|
| ① 扫描器关键函数（detect_reversal/strength_map/BASE/N6=50）| 7 处 ✓ |
| ② workflow 接入（步骤+路径+产物）| 5 处 ✓ |
| ③ 盘前报告 ③.5 接入（render_yitong_rsv50）| 3 处 ✓ |
| ④ 依赖 st_guard.py（GitHub）| HTTP 200 ✓ |
| ⑤ GitHub 扫描器含 RSV50 | 15 处 ✓ |

**链路完整，自检通过。**

---

## 五、次日验证点（9/14 周一）

1. 盘后全盘量化日志应出现「一统天下多周期扫描」步骤，且产出股池文件；
2. 9/15 盘前报告应出现 **③.5 一统天下·RSV50三线强度** 节；
3. 若步骤超时（yitong_screener 全量约 20-25 min），需检查 quant_scan 的 300 min timeout 余量。

---

## 六、附：本会话其他体系变更（9/7-9/8）

- 盘前报告 `gen_premarket_report.py`：新增**数据就绪检查**（quant 数据过期/缺失 → 降级标注 + `data_fresh` 字段）
- 盘前报告 cron：08:00 → **06:30**（对冲 GitHub cron 延迟）
- 全盘量化 cron：15:30 → **15:05**
- 两 workflow 接入 **repository_dispatch**（cron-job.org 外部精确触发，替代原生 schedule）
- 报告自检 `report_selfcheck.py`：新增 `data_fresh` 过期告警
