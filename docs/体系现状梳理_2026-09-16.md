# bg 量化体系现状梳理

> 生成时间：2026-09-16 19:20（北京时间）
> 仓库：`wuhj-hub/bg` @ main
> 数据来源：GitHub Contents API 实测 + 本地平台技能盘点

---

## 一、总览

| 维度 | 数量 | 说明 |
|---|---|---|
| Workflow | **12** | 5 个纯手动探测 + 7 个自动/可调度 |
| 脚本（.py） | **109** | quant_scripts 90 + 仓库根 19 |
| 平台技能 | **31** | 13 官方（ima-*/pdfkit 等）+ 18 自建/第三方 |
| skills_backup 备份 | **14** | 自建技能备份（与平台侧对应） |
| 仓库根文件 | **101** | 含 19 个 .py |
| 顶层目录 | **7** | .github / docs / outputs / pools / premarket_scripts / quant_scripts / skills_backup / 量学书籍 |

---

## 二、Workflow 全景（12 个）

### 自动运行链（7 个）

| Workflow | 触发（cron UTC → 北京） | 作用 |
|---|---|---|
| **quant_scan** | `repository_dispatch: quant-scan-run`（cron-job.org 外部触发） | 全量扫描主链路，**33 个步骤** |
| **quant_report** | `workflow_run: completed`（quant_scan 完成后自动） | 盘后报告 + 消费全链产物，**最大文件 31KB** |
| **premarket_report** | `30 22 * * 0-4` → **06:30（周日-周四）** | 盘前报告 |
| **intraday_monitor** | `*/15 1-7 * * 1-5` → **09:00-15:45 每15分** | 盘中预警（≤10元）|
| **wangzhe_track** | `35 7 * * 1-5` → **15:35** | 涨停型王者跟踪 |
| **market_regime** | `30 9 * * 1-5` → **17:30** | 市场状态判定（三级别力量）|
| **guard_selfcheck** | `30 1 * * *` → **09:30** | 体系自检 |
| **artifact_audit** | `0 22 * * *` → **06:00** | 产物入库审计（快照 A 产物）|
| **evidence_review** | `0 1 1 * *` → **09:00（每月1号）** | 卡位池证据月度复核 |

⚠️ **quant_scan 已移除原生 schedule**（2026-09-10 方案A），改由 cron-job.org → repository_dispatch 精确触发，避免与原生 cron 重复。

### 纯手动探测（3 个）

| Workflow | 说明 |
|---|---|
| **probe_em** | 东财端点可达性探测；**是 `sector_component_em.json` 的原始生成器** |
| **probe_kpl** | 开盘啦端点探测 |
| **probe_search** | 搜索端点探测（**从未触发过**）|

---

## 三、脚本资产（109 个 .py）

### quant_scripts/（90 个）按功能分类

**核心扫描（策略层）**
`beast_screener`（猛兽）、`fish_body_enhanced` + `gen_fish_pool`（鱼身）、`run_shuangxian`（双弦）、`yitong_screener` + `yitong_guaili_screener` + `yitong_60m_fullscan`（一统天下）、`longtou` + `dragon_leader`（龙头）、`wangzhe_track` + `wangzhe_confirm` + `wangzhe_commit`（王者）、`liangxue_screener` + `liangxue_minute_check` + `liangxue_month_join`（量学）、`reversal_2b_screener` + `reversal_funnel_screener` + `reversal_value_screener`（反转）、`scan_123_2b`、`top_signal`、`yao_gu_pool`、`caige_pool`

**市场环境（温度计层）**
`market_width`、`market_regime`、`hot_emotion` + `sync_hot_emotion` + `emotion_forecast`、`yearline_breadth` + `yearline_section`、`rsv_strength`、`monthly_macd_monitor`、`sector_divergence`、`rsg_weekly_report`

**资金/结构**
`quad_resonance`（根）、`industrial_anchor4`、`ai_chokepoint_guard`、`portfolio_risk`、`sector_components` + `ths_sector_fetch` + `runner_fetch_em_sectors`

**信号仲裁与风控**
`signal_arbiter`、`trade_guard`、`signal_risk_card`、`execution_card`、`st_guard`、`report_selfcheck`

**报告生成**
`gen_premarket_report`、`gen_review_report`、`gen_judgment`、`run_all_quant`

**数据/工具**
`fetch_*`（3 个）、`data_guard`、`artifact_audit`、`git_push`、`guard`、`gen_mainboard`、`month_frame`、`st_guard`

**回测/研究**
`backtest_*`（3 个）、`bt_*`（3 个）、`vcp_backtest`、`beast_tech_backtest`、`fish3_backtest`、`learn_weights`、`paper_tracker`、`win_rate_*`（4 个）

**其他策略**
`zuot_kuangren`（做T）、`wuwei_xianxing_*`（武威）、`baidu_pan`、`fetch_hot_kline`、`probe_em_params`、`runner_probe_kpl`

### 仓库根（19 个）

`full_market_dualdim`（**乾坤A 生成器**）、`intraday_monitor`、`market_style`、`quad_resonance`、`sector_resonance_local`、`anti_resilience`、`minute_strength`、`self_check`、`signal_arbiter`、`tdx_cross_check`、`ima_cred_check`、`push_notify`、`push_report`、`upload_ima`、`git_api_commit`、`update_changelog`、`gen_mainboard`、`market_width`、`guard_selfcheck`

---

## 四、技能资产

### 平台技能（31 个，/root/.skills）

**官方/第三方（13）**：`ima-doc`、`ima-knowledge`、`ima-note`、`ima-pdf`、`ima-podcast`、`ima-ppt`、`ima-report`、`ima-skill-creator`、`pdfkit`、`kb-root-organizer`、`serenity-masters-screener`、`westock-data`、`tencent-news`、`tencent-ima-copilot-legal-consultation`

**自建/量化（18）**：`盘前市场报告`、`复盘报告`、`猛兽体系`、`鱼身`、`双弦投资系统`、`双弦投资系统-月度股池`、`强势体系`、`板块个股入牛时点`、`个人23策略股票分析技能`、`wangbei-report`、`wuwei-report`、`xihu-report`、`bo-duan-sao-miao`、`duo-wei-du`、`fish-body-trading`、`百度网盘直读助手`、`微信读书`、`kb-root-organizer`

### 备份（skills_backup，14 个）

`bo-duan-sao-miao`、`duo-wei-du`、`fish-body-trading`、`wangbei-report`、`wuwei-report`、`xihu-report`、`个人23策略股票分析技能`、`双弦投资系统`、`复盘报告`、`强势体系`、`板块个股入牛时点`、`猛兽体系`、`盘前市场报告`、`鱼身`

⚠️ **备份与平台不一致**：平台有但无备份的自建技能 = `百度网盘直读助手`、`微信读书`、`双弦投资系统-月度股池`（后者为重复技能）

---

## 五、产物链路（核心）

```
quant_scan（33 步）
  ├─ 生成沪深主板清单 ────────→ all_mainboard.csv
  ├─ 板块成分映射（东财）──────→ sector_component_em.json（999板块/5647只）
  ├─ 全量量化扫描 ─────────────→ outputs/quant_results_{date}.json
  ├─ market_width ────────────→ market_width_latest.json
  ├─ hot_emotion ─────────────→ hot_emotion_latest.json + history
  ├─ emotion_forecast ────────→ 次日预判
  ├─ yearline_breadth ────────→ yearline_breadth_latest.json
  ├─ 双弦/鱼身/猛兽预运行 ────→ 三系统温度
  └─ 提交产物 ────────────────→ 仓库根（供盘前/报告读取）
         ↓ (workflow_run: completed)
quant_report（盘后）
  ├─ 消费 quant_results / 三系统温度
  ├─ 乾坤A（full_market_dualdim）→ 资金+业绩共振
  ├─ 四态胜率（win_rate_tracker --by-phase）
  └─ 提交 + 上传知识库「盘后量化」
         ↓
premarket_report（次日 06:30）
  └─ 读取全部 latest.json → 生成盘前报告 → gen_judgment → 推送
         ↓
gen_review_report（复盘）
  └─ 读取盘前 judgment → 验证预判 → 输出复盘
```

---

## 六、遗留问题清单

### 🔴 高优先级

| # | 问题 | 现状 | 建议 |
|---|---|---|---|
| 1 | **`gen_mainboard.py` 根目录与 quant_scripts 两份内容不同** | 根=3912B / quant_scripts=3411B | 确认真源，删冗余 |
| 2 | **同名脚本分散两处** | `market_width.py`（相同）、`signal_arbiter.py`（仅根）、`quad_resonance.py`（仅根） | 明确规范：新增放 quant_scripts |
| 3 | **乾坤A 有效性待观察** | 仅 4 个信号日，n 太小 | 积累 2-4 周后评估权重 |
| 4 | **`双弦投资系统-月度股池` 平台侧重复** | 已删 backup，平台侧仍在 | 确认是否从平台移除 |

### 🟡 中优先级

| # | 问题 | 现状 |
|---|---|---|
| 5 | **artifact_audit 61 个"未提交"** | 多为运行态产物噪声，已加白名单 |
| 6 | **probe_search 从未触发** | 是否需要保留？ |
| 7 | **才哥公众号历史文章无法枚举** | 依赖 `caige_urls.txt` 手工维护 |
| 8 | **龙头战法（dragon_leader）vs longtou 并行** | 1-2 周后决定是否融合 |
| 9 | **`仲裁信号日志.csv` 曾断链** | 已修，需观察是否持续累积 |

### 🟢 已解决（本期）

- ✅ 板块成分映射彻底修复（push2delay，0→5647 只）
- ✅ 盘中预警价格上限（≤10元，三处覆盖）
- ✅ 乾坤A 断链修复（产物入提交列表）
- ✅ 猛兽报告 3 处硬编码说明
- ✅ 鱼身温度兜底（0 + 数据缺失标记）
- ✅ 产物入库统一审计器建成

---

## 七、本次清理记录（2026-09-16）

| 项 | 处理 |
|---|---|
| `skills_backup/复盘报告_SKILL.md`（误传） | ✅ 已删 |
| `skills_backup/盘前市场报告_SKILL.md`（误传） | ✅ 已删 |
| `skills_backup/双弦投资系统-月度股池/`（重复技能，4 文件） | ✅ 已删 |
| `/root/.skills/` 11 个乱码僵尸目录（全 0B） | ✅ 已删 |
| `runner_fetch_em_sectors.py` docstring 过时表述 | ✅ 已更正 |
| `quant_scan.yml` line 81 过时注释 | ✅ 已更正 |
| agent_md「东财全域不可达」旧认知 | ✅ 已更正为 push2delay 优先 |

---

*本梳理由脚本实测数据生成，非推测。下次梳理建议在重大架构变更后。*
